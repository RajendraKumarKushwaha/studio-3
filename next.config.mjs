/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["www.mrfstudio.com","imgpanda.com", "memoriesimagesvideos.s3.us-east-1.amazonaws.com"],
    },
  }

export default nextConfig;
